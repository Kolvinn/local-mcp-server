- Opencode architecture will become redundant. WIll replace completely by langchain ecosystem

- opencode tui/server will be replaces by the deep-agents-cli that the user will use to talk to the coordinator.

- agents will be wrapped in the deep agent cli packaging such that their langchain/langraph flows are persisted and are available for bubble up permissions to the user

- flox is assumed to contain all packages. The user will deal with it if it doesnt